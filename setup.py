from setuptools import setup, find_packages

setup(
    name='hypersim',
    version='0.1.0',
    description='Simulate and visualize hyperspectral galaxy data',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'matplotlib'
    ],
)

